# ayeshooo

This project uses **React + Vite** with TailwindCSS and ESLint configured.  
It provides a minimal setup to get React working in Vite with Hot Module Replacement (HMR) and some ESLint rules.

## Features

- React + Vite for fast development and build
- TailwindCSS for utility-first styling
- ESLint for code quality
- React Router ready for SPA routing
- Assets and components structure pre-configured

## React Compiler

The React Compiler is not enabled by default because of its impact on dev & build performance.  
To enable it, see the [React Compiler documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled.  
Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for info on integrating TypeScript and [`typescript-eslint`](https://typescript-eslint.io).

## Getting Started

1. Clone the repository:
```bash
git clone https://github.com/ayeshooojee143/ayeshooo.git
